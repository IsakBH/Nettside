// apply dark mode når siden lastes
document.addEventListener('DOMContentLoaded', () => {
    applyDarkMode();  // Pass på at applyDarkMode blir kalt på settings-siden
    console.log("Dark mode applied on settings page");  // Legg til en logg for feilsøking
});
